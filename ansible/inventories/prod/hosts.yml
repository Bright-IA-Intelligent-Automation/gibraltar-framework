all:
  children:
    ignition_servers:
      hosts:
        customer-ignition-server:
          ansible_host: 192.0.2.10
          ansible_user: deploy
          ansible_become: true
          ignition_container_name: ignition
          ignition_volume_name: ignition_data
          ignition_release_path: /opt/ignition-release
          ignition_backup_path: /opt/ignition-backups
